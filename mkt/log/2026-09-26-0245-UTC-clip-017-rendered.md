# 2026-09-26 02:4x UTC — 5s toolchain test clip (017) rendered from real app + code

- `mkt/video/017-toolchain-test-5s.mp4` — 5.000s, 1280x720, 30fps, H.264 + AAC, 227 KB.
- Built with the new pipeline only: `frames.html` (canvas scene) + `render-clip.py` (Playwright frame capture, numpy/scipy score, ffmpeg encode, Lanczos ×2 supersample).
- Real product footage: the live v0.9.36 onboarding/settings window captured from the running app (`caps/onboarding.png`), composited as a sheared 3D panel.
- The ear is the app's own glyph (U+1F442, ported from `paint.rs:150` paint_glyph — grayscale + shadow/glow) with the real listening ring (120-bucket circular waveform, ported from `paint.rs:63`).
- Score synthesised in numpy/scipy: sub hit, filtered noise riser, pad, click, impact + shimmer tail. No stock audio, no licences.
- Research-driven fixes this round: (1) PrintWindow/PW_RENDERFULLCONTENT cannot capture layered/transparent windows (the floating ear) — Windows.Graphics.Capture is the documented answer; (2) `drawImage` from an `<img>` with a fractional source rect silently drops the draw in Chromium — pre-scale to an offscreen canvas then shear; (3) ×2 supersampling for type crispness; (4) per-beat still review as a render gate.
- Cost: 0. Public post: NO (internal test, not published anywhere).
