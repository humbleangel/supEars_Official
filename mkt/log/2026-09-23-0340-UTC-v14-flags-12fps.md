# 2026-09-23 03:40 UTC — v14: flags under languages + pro post-processing (12fps)

- Owner order: flags under each language name (1-2, case by case; keep all text as is) + full-frame subtle blur + light noise + render at 12fps.
- Flags: 29 flagcdn PNGs (public domain) in `mkt/video/flags/`, mapped per language in `render.ps1` ($FlagMap: English=gb+us, Portuguese=br+pt, Spanish=es+mx, Arabic=sa+eg, rest single). Per-language strips pre-composited with a dark gap, overlaid under the language name with slot fades. Owner renamed langs.txt "Chinese (Simplified)" → "Chinese" mid-flight; map updated.
- Bug found + fixed during render: flag inputs were single-frame streams, so timeline fades evaluated at t=0 and the flags never appeared; now looped like the logo/emblem. Verified frames: UK+US under English, France in vertical.
- Post-processing: `gblur=sigma=0.5` (full frame), `noise=alls=5:allf=t`, output 12fps (also inputs/zoompan at 12).
- Rendered `mkt/video/014-seventeen-days-v14.mp4` + `-vertical.mp4` (40s, 17 days).
- Cost: 0. Public post: NO (awaiting owner approval).
